import React from "react";

const Footer = () => {
  return (
    <footer
      style={{
        backgroundColor: "#111",
        color: "#fff",
        padding: "1rem",
        textAlign: "center",
      }}
    >
      Footer OK
    </footer>
  );
};

export default Footer;
